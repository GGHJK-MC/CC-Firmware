local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()local url = "https://raw.githubusercontent.com/GGHJK-MC/CC-Firmware/refs/heads/master/sbrom.lua"

local function crash()
    print("S_BROM_DOWNLOAD_FAIL_5808")
    while true do
        os.pullEventRaw()
    end
end

local oldPull = os.pullEvent
os.pullEvent = os.pullEventRaw

local h = http.get(url)
if not h then crash() end

local code = h.readAll()
h.close()

os.pullEvent = oldPull

local fn, err = load(code)
if not fn then
    print(err)
    crash()
end

fn()
